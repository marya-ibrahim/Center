
  # Library Management System

  Frontend library management system built with React and React Bootstrap for modern UI components

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  
